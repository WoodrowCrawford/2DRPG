using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TouchLeftBehavior : MonoBehaviour
{
    [SerializeField]
    //The speed at how fast the player will move to the left
    private float _movespeed = 2;

    //Gets a reference to the player movement behavior
    private PlayerBehavior _player;

    // Start is called before the first frame update
    void Start()
    {
     
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
